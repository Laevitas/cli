bin/
dist/
*.exe
*.exe~
.DS_Store
.env
.env.*
*.pem
*.key
*.p12
*.pfx
config.json
x402-token
.vscode/

# Editor / agent tooling
.codex/

# Local Go build cache (when GOCACHE is overridden into the worktree)
.cache-go/

# Bare binary built by `go build -o laevitas` (no extension on Linux/WSL)
/laevitas

# Local debug logs
*.log

# Generated by .github/workflows/deploy-site.yml — root install.{ps1,sh} are
# copied here at deploy time so cli.laevitas.ch serves the canonical scripts.
site/install.ps1
site/install.sh
